namespace Engine.Render
{ public class VertexAttribute
    {
        public int Location; // Attribute location in shader
        public int Size;     // Number of components (e.g., 3 for vec3)
        public int Offset;   // Offset in bytes
    }
}
